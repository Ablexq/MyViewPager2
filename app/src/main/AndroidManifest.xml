<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myandroidx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:maxAspectRatio="2.4"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning"
        tools:targetApi="o">

        <!--全面屏-->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.4" />

        <!--适配华为（huawei）刘海屏-->
        <meta-data
            android:name="android.notch_support"
            android:value="true" />
        <!--适配小米（xiaomi）刘海屏-->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />

        <activity android:name=".activity.viewpager.ViewPager2TablayoutActivity" />
        <activity android:name=".activity.viewpager.ViewPagerTablayoutActivity" />
        <activity android:name=".activity.viewpager.ViewPager2SimpleActivity" />
        <activity android:name=".activity.CoordinateActivity" />
        <activity android:name=".activity.drawlayout.DrawerLayoutActivity" />
        <activity android:name=".activity.drawlayout.DrawerLayout_OneActivity" />
        <activity android:name=".activity.drawlayout.DrawerLayout_OtherActivity" />
        <activity android:name=".activity.drawlayout.DrawerLayout_BelowToolbarActivity" />

        <activity android:name=".activity.IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>